"""
Model configuration for regex processor.
"""

# Define configurations for different model options
# For regex, we don't actually use different models, but we maintain this for compatibility
MODEL_CONFIGS = {
    "default": {
        "name": "default",
        "description": "Standard regex pattern matching",
    },
}
